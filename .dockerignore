# Without ** the pattern only catches the top level. Since server/ exists,
# its node_modules would otherwise have travelled into the build context and
# overwritten the dependencies freshly installed inside the image.
**/node_modules
dist
.git
.DS_Store
caddy/
.env
*.local
HANDOFF.md
ADAPTER_HOWTO.md
RECON.md
